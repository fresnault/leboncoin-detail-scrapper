version: '2'
services:
    leboncoindetailscrapper-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb leboncoindetailscrapper-mongodb-config
    leboncoindetailscrapper-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    leboncoindetailscrapper-mongodb-config:
        image: mongo:4.0.2
        container_name: leboncoindetailscrapper-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
